{
  "name": "opencampus-root",
  "type": "module",
  "private": true,
  "scripts": {
    "_prepare": "git submodule update --init --recursive",
    "admin": "pnpm --filter ./admin",
    "bootstrap": "pnpm --filter ./bootstrap",
    "portal": "pnpm --filter ./portal"
  },
  "engines": {
    "node": ">=20.14.0"
  }
}
